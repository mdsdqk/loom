*/resume*.pdf
*linkedin*export*
# ...but the network-scan test fixtures are synthetic, not a real export, and
# the suite cannot run without them.
!tools/test/fixtures/linkedin-export/
!tools/test/fixtures/linkedin-export/**

node_modules
dist
.turbo

#network scan http cache
.cache

#candidate files
candidate/*
![candidate/.gitkeep]

#opportunity files
opportunities/*
![opportunities/.gitkeep]

#creds
.env
**/.env